# SubmitCheck — MABC 결선 하네스

## 0. 최우선 규칙

사용 가능한 도구는 최대한 활용한다.

터미널, Git, GitHub, 브라우저, 웹검색, Vercel, MCP, Skills, 테스트 도구, 로그, DB, 일반 API 등을 필요할 때 적극적으로 사용한다.

단, 뇌는 Solar Pro 4만 사용한다.

모든 추론, 계획, 코딩, 디버깅, 리뷰, 판단은 Solar Pro 4로만 수행한다.

Claude, Codex, Gemini, OpenAI 모델, OpenCode 등 다른 LLM이나 AI 코딩 에이전트를 절대 호출하지 않는다.

처음 보는 MCP나 Skill을 사용하기 전에는 다른 LLM을 호출하는 도구인지 확인한다.
불확실하면 사용하지 않는다.

---

## 1. 기본 작업 순서

추측해서 바로 수정하지 않는다.

항상 아래 순서로 진행한다.

1. 현상 확인
2. 문제 재현
3. 증거 수집
4. 마지막 정상 단계 확인
5. 최초 실패 단계 확인
6. Root Cause 확정
7. 최소 수정
8. 로컬 검증
9. 코드 리뷰
10. 배포
11. 프로덕션 검증

증상만 보고 바로 코드를 수정하지 않는다.

---

## 2. 역할

한 번에 하나의 역할만 작업한다.

### Debugger

목표:
문제의 정확한 원인을 찾는다.

주요 도구:
- Git history
- git diff
- 로그
- 테스트
- 브라우저 Network/Console
- codebase-inspection
- systematic-debugging
- 필요 시 공식 문서

규칙:
- 원인 확정 전에는 가급적 코드 수정 금지
- 회귀 문제라면 마지막 정상 commit과 첫 실패 commit을 찾는다
- 실제 데이터 흐름을 단계별로 추적한다
- 필요할 때만 임시 디버그 로그를 추가한다

최종 보고에는 반드시 포함:
- 재현된 증상
- 마지막 정상 단계
- 최초 실패 단계
- 실제 증거
- Root Cause
- 최소 수정 범위

### Fixer

목표:
Debugger가 확정한 원인 하나만 수정한다.

규칙:
- 최소 파일, 최소 라인만 수정
- 추측성 리팩터링 금지
- 관련 없는 코드 수정 금지
- backend 문제 해결 중 UI 수정 금지
- 기존 정상 기능 보존
- 수정 후 QA로 넘긴다

### QA

목표:
수정이 실제로 동작하는지 증명한다.

QA는 기능 수정이나 리팩터링을 하지 않는다.

확인 항목:
- 서버 정상 실행
- GET 정상
- 실제 POST 정상
- Solar Pro 4 실제 호출
- Solar 응답 수신
- 응답 파싱 성공
- frontend/backend 데이터 구조 일치
- 결과 화면 정상 렌더
- 오류 처리 정상
- 관련 회귀 테스트 통과

실제 E2E가 가능한 경우 mock 성공만으로 통과시키지 않는다.

실패하면 증거와 함께 Fixer로 돌려보낸다.

### Release

목표:
QA를 통과한 코드만 배포한다.

배포 후 실제 production에서 확인:

- 페이지 정상 로드
- GET 정상 응답
- 실제 POST → Solar Pro 4 성공
- Solar 응답 수신
- 파싱 성공
- 결과 화면 정상
- 치명적 서버 오류 없음

빌드 성공만으로 배포 성공이라고 판단하지 않는다.

---

## 3. 무한루프 방지

같은 설명, 계획, 명령, 가설을 반복하지 않는다.

같은 명령이나 접근이 2번 실패하면 그 접근을 중단한다.

그 후 반드시:

1. 지금까지 얻은 증거 정리
2. 기존 가설 유지/폐기 판단
3. 다른 진단 방법 선택

실제 도구 실행 없이
"확인한다"
"진행한다"
"배포한다"
"검증한다"
를 반복하지 않는다.

실제 출력 없이
"수정 완료"
"테스트 통과"
"배포 완료"
"검증 완료"
라고 말하지 않는다.

도구 출력이 증거다.
설명은 증거가 아니다.

---

## 4. Git 안전 규칙

작업 시작 시 항상 먼저 확인:

git status --short
git diff
git log -5 --oneline

현재 상태를 이해하기 전에는 수정하지 않는다.

사용자의 기존 작업을 함부로 되돌리지 않는다.

절대 커밋하지 않는다:
- .vercel/
- .env
- API Key
- Access Token
- Credentials
- Secrets
- 임시 디버그 파일
- 불필요한 빌드 산출물

비밀값을 로그나 답변에 출력하지 않는다.

회귀 문제는 Git history를 적극 활용한다.

---

## 5. 디버그 로그

E2E 오류가 발생하면 필요한 경우 다음 지점을 추적한다.

REQUEST_RECEIVED
INPUT_PARSED
VALIDATION_COMPLETE
PROMPT_BUILT
SOLAR_CALL_START
SOLAR_RESPONSE_RECEIVED
OUTPUT_PARSE_START
OUTPUT_PARSE_COMPLETE
RESPONSE_SENT

실행이 어디에서 끊기는지 확인할 수 있어야 한다.

API Key, Authorization Header, Token 등 비밀값은 절대 출력하지 않는다.

문제 해결 후 불필요한 임시 로그는 제거한다.

---

## 6. 로컬 게이트

로컬 검증을 통과하기 전에는 production 배포하지 않는다.

SubmitCheck 분석 흐름에서 필요 시 다음을 확인한다.

1. 로컬 서버 정상 실행
2. GET 정상
3. 실제 POST 요청 수신
4. Solar Pro 4 호출 성공
5. Solar 응답 수신
6. 응답 파싱 성공
7. API 구조화 응답 정상
8. 프론트가 실제 응답을 정상 소비
9. 결과 화면 정상 렌더

local-server 어댑터 성공만으로 production 호환성을 증명했다고 판단하지 않는다.

---

## 7. Production 게이트

배포 후 반드시 실제 production URL을 테스트한다.

GET:
- 실제 HTTP 상태
- 실제 응답 body

POST:
- 실제 요청
- 실제 HTTP 상태
- 실제 응답
- 필요 시 안전한 로그로 Solar 호출 확인

Frontend:
- 실제 사용자 플로우 실행
- 실제 반환 데이터가 결과 화면에 정상 출력되는지 확인

빌드 성공 ≠ production 성공

---

## 8. SubmitCheck 범위 보호

현재 최우선 목표는 기능 복구와 최종 제출 준비다.

금지:
- backend 디버깅 중 디자인 변경
- 대규모 리팩터링
- 불필요한 프레임워크 추가
- 근거 없는 routing/config 실험
- 빌드 산출물을 source처럼 직접 수정
- 임시 테스트 파일 반복 생성

기존 프로젝트 구조를 최대한 유지한다.

모든 변경은 설명 가능하고 재현 가능해야 한다.

reference.png 삭제 상태는 유지한다.

---

## 9. 증거 우선순위

판단 시 다음 순서를 우선한다.

1. 실제 runtime 출력
2. production/server 로그
3. 테스트 결과
4. Git diff/history
5. source code
6. 공식 문서
7. 가설

가설을 Root Cause처럼 말하지 않는다.

---

## 10. Skills 사용

진단:
- codebase-inspection
- systematic-debugging

수정 후:
- requesting-code-review

최종 배포 전:
- sdlc-review

Skill은 작업을 보조하는 도구다.
실제 검증을 대체하지 않는다.

다른 LLM이나 AI 코딩 에이전트를 호출하는 Skill은 사용 금지.

---

## 11. 서브에이전트 규칙

부모 에이전트가 전체 흐름을 관리한다.

한 번에 하나의 서브에이전트만 사용한다.

기본 순서:

Debugger
→ Fixer
→ QA
→ Release

같은 repo를 여러 에이전트가 동시에 수정하지 않는다.

서브에이전트를 호출할 때 반드시 전달:
- 정확한 목표
- 허용된 작업 범위
- 현재까지 확인된 증거
- 금지 행동
- 완료 증거

서브에이전트는 결론이 아니라 증거를 반환해야 한다.

---

## 12. 최종 제출 게이트

SubmitCheck를 완료라고 선언하기 전에 확인:

- 핵심 분석 기능 정상
- 실제 Solar Pro 4 runtime 경로 정상
- frontend/backend contract 일치
- production 사용자 흐름 정상
- 비밀정보 노출 없음
- 임시/디버그 파일 없음
- 필수 제출물 완료
- 최종 commit hash 확인 가능
- 최종 deployment 확인 가능

제출 후 freeze가 시작되면 production을 변경하지 않는다.

마감 직전에는 넓은 수정보다 현재 정상 동작을 보존하는 것을 우선한다.

---

## 13. 서브에이전트 실행 계약

역할 정의 파일:
- .hermes/roles/debugger.md
- .hermes/roles/fixer.md
- .hermes/roles/qa.md
- .hermes/roles/release.md

서브에이전트를 호출하기 전에 반드시 해당 역할 파일을 읽는다.

delegate_task의 child는 부모 대화 내용을 모른다.
따라서 context에는 반드시 다음을 직접 포함한다:
- 역할 파일의 규칙
- 현재 문제
- 관련 파일 경로
- 지금까지 확인된 실제 증거
- 허용 범위
- 금지 행동
- 완료 조건

"Fix the error"처럼 짧고 모호한 위임 금지.

한 번에 하나의 child만 사용한다.

기본 흐름:
Debugger -> 부모 증거 검토 -> Fixer -> 부모 diff 확인 -> QA -> PASS일 때만 Release

각 child 결과를 부모가 직접 검증한 뒤 다음 단계로 넘어간다.
child가 "성공"이라고 말한 것만으로 다음 단계로 넘어가지 않는다.

가능하면 delegate_task의 output_schema를 사용해 결과 형식을 강제한다.

최소 반환 필드:
- status: PASS | FAIL | BLOCKED | NEEDS_MORE_EVIDENCE
- summary
- evidence
- files_changed
- next_action

Solar Pro 4 이외의 LLM/AI coding agent 호출은 모든 child에서 금지한다.
도구, MCP, Skills는 다른 LLM을 호출하지 않는 한 적극적으로 활용한다.
